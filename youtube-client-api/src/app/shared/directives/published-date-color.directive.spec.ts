import { PublishedDateColorDirective } from './published-date-color.directive';

describe('PublishedDateColorDirective', () => {
  it('should create an instance', () => {
    const directive = new PublishedDateColorDirective();
    expect(directive).toBeTruthy();
  });
});
